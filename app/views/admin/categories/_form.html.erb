<div class="form">
  <div class="field-wrap">
    <%= form_for [:admin, @category] do |f| %>
      <%= f.label :title, "Create a category" %><br>
      <%= f.text_field :title %><br>
      <%= f.submit %>
    <% end %>
  </div>
</div>
