<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Idea</title>
  </head>
  <body>
    <h2>Idea:</h2>
    Idea Title: <%= @idea.title %><br>
    Idea Content: <%= @idea.body %><br>
    Idea Category: <%= @idea.category.title %><br>
    <p><%= link_to "Go Back to All Ideas", user_path(current_user) %></p>
    <p><%= link_to "Delete", user_idea_path(current_user, @idea), method: :delete %></p>
    <p><%= link_to "Edit", edit_user_idea_path(current_user, @idea) %></p>
    <p><%= link_to "Back to #{current_user.username} homepage", user_path(current_user) %></p>
  </body>
</html>
