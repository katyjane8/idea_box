<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h3>Ideas!</h3>
    <% @ideas.each do |idea| %>
    <%= link_to idea.title %>
    <%= idea.body %>
    <% end %>
    <p><%= link_to "Create a New Idea", new_user_idea_path(current_user) %> </p>
  </body>
</html>
