<!DOCTYPE html>
<html>
  <head>
  <!-- /*<style style="text/css">
    body {
      background-image: url("../assets/images/PaperAirplanes.jpeg");
      }
      </style>*/ -->
    <meta charset="utf-8">
    <title></title>
  </head>
    <body class="show">
    <h1>Welcome, <%= current_user.username %>!</h1>

    <%= link_to "Create Idea", new_user_idea_path(current_user) %>

    <% @ideas.each do |idea| %>
    <h2><%= link_to idea.title, user_idea_path(current_user, idea) %></h2>
    <p>Posted on: <%= idea.created_at.strftime("%b %d, %Y") %></p>
    <!-- <%= idea.created_at.strftime("%H:%M %P") %></p> -->
    <% end %>
  </body>
</html>
